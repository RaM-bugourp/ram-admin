# ram-adminx P1/P2 修复汇总

**日期**: 2026-06-09
**范围**: 4 个 P1 + 4 个 P2

---

## 修复清单

### P2 #2: is_deleted 用户仍可登录 ✅
- `backends.py`: `User.objects.get(username=username)` → `is_deleted=False`
- 已验证: 软删除用户 login → 401

### P2 #4: list_users 不过滤 is_deleted ✅
- `user_service.py`: `User.objects.order_by()` → `is_deleted=False`
- 已验证: 用户列表仅展示非删除用户

### P2 #2: last_login vs last_login_at 双字段 ✅
- `auth_views.py`: 移除 `timezone.now()` 导入和 `last_login_at` 手动赋值
- 依赖 Django 内置 `login()` → `user_logged_in` 信号 → 自动更新 `last_login`

### P2 #1: 前端 store 未存储角色 ✅
- `stores/user.ts`: 新增 `roles: RoleInfo[]`、`isAdmin` getter、`roleCodes` getter
- `login` / `fetchUserInfo` actions 均保存 roles

### P1 #1: 前端未按角色隐藏写操作按钮 ✅
- `UserListView.vue`: 新建按钮 + 编辑/删除/重置密码 → `v-if="isAdmin"`
- `RoleListView.vue`: 新建按钮 + 编辑/删除 → `v-if="isAdmin"`
- 非管理员看到 "—" 占位符

### P1 #2: Dashboard 数据占位 "——" ✅
- 新建 `dashboard_views.py` + `urls_dashboard.py`
- `DashboardView.vue`: 接入 `/api/dashboard/stats/` 实时数据

### P1 #3: audit 应用空壳 - 暂缓
- 保留两个空文件，待业务需求驱动

### P1 #4: RolePermission 未使用 - 暂缓
- 表已建，待权限粒度需求驱动

---

## 改动文件统计

| 层 | 新建 | 修改 |
|----|------|------|
| 后端 | 3 | 4 |
| 前端 | 2 | 4 |

### 后端
- `backends.py` — +is_deleted 过滤
- `user_service.py` — +is_deleted 过滤
- `auth_views.py` — 移除 last_login_at，login 返回 roles
- `dashboard_views.py` — **新建** Dashboard 统计
- `urls_dashboard.py` — **新建** 路由
- `config/urls.py` — +dashboard 挂载

### 前端
- `stores/user.ts` — +roles + isAdmin getter（重写）
- `UserListView.vue` — isAdmin 条件隐藏按钮
- `RoleListView.vue` — isAdmin 条件隐藏按钮
- `DashboardView.vue` — 接入实时数据
- `api/modules/dashboard.ts` — **新建**

### 验证结果
- ✅ TypeScript 零错误
- ✅ 软删除用户登录被拒 (401)
- ✅ user-info 返回 roles
- ✅ Dashboard 返回实时统计
- ❌ 未验证（P1#3/P1#4 暂缓）
